import {readFile,mkdir,writeFile} from 'node:fs/promises';import path from 'node:path';import {DataForSeoMerchantApiClient,DataForSeoAcquisitionService,loadDataForSeoCredentials,createSellersEnrichmentProposal} from '../packages/mercury/index.js';
const args=new Map(process.argv.slice(2).map(x=>{const i=x.indexOf('=');return i<0?[x,true]:[x.slice(0,i),x.slice(i+1)]}));const productInfoTaskId=args.get('--product-info-task-id');if(typeof productInfoTaskId!=='string')throw new Error('PRODUCT_INFO_TASK_ID_REQUIRED');
const stateRoot=path.resolve('.forge-review/acquisition');const auth=JSON.parse(await readFile(path.join(stateRoot,'product-info-authorization-request.json'),'utf8'));
const credentials=loadDataForSeoCredentials();const transport=async({method,url,headers,body})=>{const r=await fetch(url,{method,headers,body:body?JSON.stringify(body):undefined});const data=await r.json();if(!r.ok)throw new Error(`HTTP_${r.status}`);return data};const client=new DataForSeoMerchantApiClient({login:credentials.login,password:credentials.password,transport});const service=new DataForSeoAcquisitionService({client});const result=await service.getProductInfoResult(productInfoTaskId);const proposal=createSellersEnrichmentProposal({productInfoTaskId,productInfoResult:result,productInfoAuthorization:auth});const envelope={schemaVersion:'1.0',proposal,paidTaskCreated:false,actualSpendUsd:0};const out=path.join(stateRoot,'sellers-enrichment-proposal.json');await mkdir(path.dirname(out),{recursive:true});await writeFile(out,JSON.stringify(envelope,null,2)+'\n');
console.log('SELLERS ENRICHMENT PREPARE');console.log('Source PRODUCTS task:   ',proposal.sourceProductsTaskId);console.log('Source PRODUCT_INFO task:',proposal.sourceProductInfoTaskId);console.log('Atlas product:          ',proposal.atlasProductId);console.log('Data docid:             ',proposal.providerIdentity.dataDocId);console.log('Product Info evidence:   VALIDATED');console.log('Operation:               SELLERS');console.log('Paid task created:       NO');console.log('Actual spend:            $0.000');console.log('Status:                  PENDING_OPERATOR_REVIEW');console.log('Proposal export:         .forge-review\\acquisition\\sellers-enrichment-proposal.json');
