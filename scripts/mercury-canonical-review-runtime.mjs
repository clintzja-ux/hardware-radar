import path from "node:path";
import {CanonicalObservationReviewAssessmentService,FileCanonicalObservationReviewAuthorizationRepository,FileReviewDecisionRepository,ObservationReviewService,ReviewWorkflowService} from "../packages/mercury/index.js";
import {createCanonicalOperatorRuntime} from "./mercury-canonical-admission-runtime.mjs";

export function createCanonicalReviewRuntime(args){const canonical=createCanonicalOperatorRuntime(args),location=(name,fallback)=>path.resolve(String(args.get(name)||fallback)),acceptanceRepository=canonical.canonicalAdmissionService.observationRepository,reviewService=new ObservationReviewService({acceptanceRepository}),reviewRepository=new FileReviewDecisionRepository({statePath:location("--review-state",".forge-review/mercury/review-decisions.json"),acceptanceRepository,environment:"production"}),reviewWorkflowService=new ReviewWorkflowService({reviewRepository,reviewService}),assessmentService=new CanonicalObservationReviewAssessmentService({acceptanceRepository,reviewService,reviewRepository,canonicalAdmissionService:canonical.canonicalAdmissionService,canonicalAuthorizationRepository:canonical.authorizationRepository}),authorizationRepository=new FileCanonicalObservationReviewAuthorizationRepository({statePath:location("--review-authorization-state",".forge-review/mercury/canonical-review-authorizations.json")});return{assessmentService,authorizationRepository,reviewWorkflowService};}
