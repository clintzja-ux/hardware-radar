import path from "node:path";import {CloudflareRecipientVerificationEvidenceAdmissionService,FileCloudflareDestinationVerificationEvidenceRepository,FileCloudflareRecipientVerificationObservationRepository,GatewayAlertOperatorRecipientApprovalRepository,GatewayAlertOperatorRecipientConfigurationRepository} from "../packages/gateway/index.js";
const args=new Map(process.argv.slice(2).map(x=>{const i=x.indexOf("=");return i<0?[x,true]:[x.slice(0,i),x.slice(i+1)];})),observedBy=args.get("--observed-by");const evidenceRepository=new FileCloudflareDestinationVerificationEvidenceRepository({filePath:path.resolve(".forge-review/gateway-alert-recipient-verification-evidence.json")}),result=await new CloudflareRecipientVerificationEvidenceAdmissionService({observationRepository:new FileCloudflareRecipientVerificationObservationRepository({filePath:path.resolve(".forge-review/gateway-alert-recipient-verification-observation.json")}),evidenceRepository,approvalRepository:new GatewayAlertOperatorRecipientApprovalRepository()}).admit({observedBy,recipientAddress:process.env.BEACON_ALERT_RECIPIENT});const projection=await new GatewayAlertOperatorRecipientConfigurationRepository({runtimeConfiguration:{},verificationEvidence:result.evidence}).getConfiguration();console.log("CLOUDFLARE RECIPIENT VERIFICATION EVIDENCE ADMISSION");console.log("Admission:                       ",result.status);console.log("Recipient:                       REDACTED");console.log("Verification state:             ",projection.verificationState);console.log("Provider deployed:               NO");console.log("Sender/domain configured:        NO");console.log("Sending enabled:                 NO");console.log("Network request:                 NONE");console.log("Actual spend:                    $0.000");
