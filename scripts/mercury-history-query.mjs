import path from "node:path";
import {FileHistoricalObservationRepository,HistoricalObservationIntelligence} from "../packages/mercury/index.js";

const args=new Map(process.argv.slice(2).map(x=>{const i=x.indexOf("=");return i<0?[x,true]:[x.slice(0,i),x.slice(i+1)];}));const atlasProductId=args.get("--atlas-product");if(typeof atlasProductId!=="string"||atlasProductId.trim()==="")throw new Error("ATLAS_PRODUCT_REQUIRED");const statePath=path.resolve(String(args.get("--state")||".forge-review/mercury/historical-observations.json"));const query={atlasProductId:atlasProductId.trim()};if(args.has("--retailer"))query.retailerId=String(args.get("--retailer"));if(args.has("--from"))query.from=String(args.get("--from"));if(args.has("--to"))query.to=String(args.get("--to"));const result=await new HistoricalObservationIntelligence({repository:new FileHistoricalObservationRepository({statePath})}).query(query);const money=value=>value==null?"NOT_AVAILABLE":`$${value.toFixed(2)}`;
console.log("MERCURY HISTORICAL INTELLIGENCE");console.log("");console.log("Atlas product:            ",query.atlasProductId);console.log("Observations:             ",result.observationCount);console.log("Retailers:                ",result.retailerCount);console.log("Currency:                 ",result.currencies.length?result.currencies.join(", "):"NONE");console.log("");console.log("First observation:        ",result.firstObservationTime??"NOT_AVAILABLE");console.log("Latest observation:       ",result.latestObservationTime??"NOT_AVAILABLE");console.log("");console.log("Latest base price:        ",money(result.latestBasePrice));console.log("Latest total price:       ",money(result.latestTotalPrice));console.log("Lowest base price:        ",money(result.lowestBasePrice));console.log("Highest base price:       ",money(result.highestBasePrice));console.log("");console.log("Trend:                    ",result.trendState);console.log("Change from previous:     ",result.changeFromPrevious==null?"NOT_AVAILABLE":money(result.changeFromPrevious.basePrice));console.log("Change from first:        ",result.changeFromFirst==null?"NOT_AVAILABLE":money(result.changeFromFirst.basePrice));console.log("");console.log("Paid task created:        NO");console.log("Actual spend:             $0.000");
