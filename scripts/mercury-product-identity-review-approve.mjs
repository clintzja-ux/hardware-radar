import {readFile} from "node:fs/promises";
import path from "node:path";
import {FileDataForSeoMarketEvidenceRepository,FileIdentityReviewDecisionRepository,IdentityReviewService} from "../packages/mercury/index.js";

const args=new Map(process.argv.slice(2).map(x=>{const i=x.indexOf("=");return i<0?[x,true]:[x.slice(0,i),x.slice(i+1)];}));
const required=name=>{const value=args.get(name);if(typeof value!=="string"||value.trim()==="")throw new Error(`${name.slice(2).replaceAll("-","_").toUpperCase()}_REQUIRED`);return value;};
const requestId=required("--request-id");const confirmationToken=required("--confirm");const reviewedBy=required("--reviewed-by");const reason=required("--reason");const contradictionStatus=required("--contradiction-status");const atlasProductId=required("--atlas-product");
const requestPath=path.resolve(String(args.get("--request")||`.forge-review/identity-review/product-${atlasProductId}.json`));const evidencePath=path.resolve(String(args.get("--evidence-state")||".forge-review/acquisition/dataforseo-market-evidence.json"));const decisionsPath=path.resolve(String(args.get("--decision-state")||".forge-review/identity-review/identity-review-decisions.json"));
const request=JSON.parse(await readFile(requestPath,"utf8"));const evidenceRepository=new FileDataForSeoMarketEvidenceRepository({statePath:evidencePath});const decisionRepository=new FileIdentityReviewDecisionRepository({statePath:decisionsPath});const service=new IdentityReviewService({evidenceRepository,decisionRepository});
const decision=await service.approvePreparedProductRequest({request,requestId,confirmationToken,reviewedBy,reason,contradictionStatus,reviewedAt:new Date().toISOString(),atlasProductId});
console.log("PRODUCT IDENTITY REVIEW APPROVAL");console.log("Request ID:             ",requestId);console.log("Decision ID:            ",decision.identityReviewDecisionId);console.log("Atlas product:          ",decision.atlasProductId);console.log("Transition:             ",`${decision.previousState} -> ${decision.requestedState}`);console.log("Outcome:                ",decision.decisionOutcome);console.log("Retained evidence changed: NO");console.log("Promotion authorized:   NO");console.log("Paid task created:      NO");console.log("Actual spend:           $0.000");
