EDR-008

Repositories return domain objects, never storage representations.

Meaning:

Consumers never care whether data comes from:

JSON
SQLite
PostgreSQL
API
generated files

Only repositories know that.

This is a surprisingly powerful abstraction because it lets us evolve the storage layer without affecting callers.