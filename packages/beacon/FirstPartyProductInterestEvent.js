export const FIRST_PARTY_INTEREST_EVENT_TYPES=Object.freeze(["OUTBOUND_RETAILER_CLICK"]);
export const FIRST_PARTY_INTEREST_SOURCE_SURFACES=Object.freeze(["OVERALL","CATEGORY_DDR5","CATEGORY_DDR4","LAPTOP_RAM"]);
const allowed=new Set(["schemaVersion","eventId","atlasProductId","signalType","sourceSurface","occurredAt","retailerId"]),pii=new Set(["name","email","ip","ipAddress","userId","accountId","advertisingId","fingerprint","userAgent","sessionId","visitorId","location","metadata"]),nonBlank=value=>typeof value==="string"&&value.trim()!=="",validTime=value=>nonBlank(value)&&Number.isFinite(Date.parse(value)),freeze=value=>{if(value&&typeof value==="object"&&!Object.isFrozen(value)){Object.freeze(value);for(const child of Object.values(value))freeze(child);}return value;};
export function validateFirstPartyProductInterestEvent(input,{maximumPayloadBytes=2048}={}){let bytes;try{bytes=Buffer.byteLength(JSON.stringify(input),"utf8");}catch{throw new TypeError("FIRST_PARTY_INTEREST_EVENT_MALFORMED");}if(!input||typeof input!=="object"||Array.isArray(input)||bytes>maximumPayloadBytes||Object.keys(input).some(key=>!allowed.has(key)||pii.has(key))||input.schemaVersion!=="1.0"||!/^[-A-Za-z0-9_]{8,128}$/.test(input.eventId)||!nonBlank(input.atlasProductId)||!FIRST_PARTY_INTEREST_EVENT_TYPES.includes(input.signalType)||!FIRST_PARTY_INTEREST_SOURCE_SURFACES.includes(input.sourceSurface)||!validTime(input.occurredAt)||!/^RETAILER-[0-9]{4}$/.test(input.retailerId))throw new TypeError("FIRST_PARTY_INTEREST_EVENT_INVALID");return freeze(structuredClone(input));}
export default validateFirstPartyProductInterestEvent;
