import assert from "node:assert/strict"; import fs from "node:fs"; import path from "node:path"; import { fileURLToPath } from "node:url";
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),".."); const files=fs.readdirSync(path.join(root,"acquisition","dataforseo")); assert.ok(files.length>=5); const text=files.map(f=>fs.readFileSync(path.join(root,"acquisition","dataforseo",f),"utf8")).join("\n"); assert.doesNotMatch(text,/merchant\/amazon/); assert.doesNotMatch(text,/DATAFORSEO_PASSWORD\s*=\s*["'][^"']+/); console.log("DataForSEO isolation tests passed.");
