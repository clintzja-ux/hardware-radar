import assert from"node:assert/strict";import crypto from"node:crypto";import{HistoricalBootstrapLifecycleService,HISTORICAL_BOOTSTRAP_CHECKPOINT_VERSION,projectHistoricalBootstrapCheckpoint}from"../index.js";const stable=v=>Array.isArray(v)?`[${v.map(stable).join(",")}]`:v&&typeof v==="object"?`{${Object.keys(v).sort().map(k=>`${JSON.stringify(k)}:${stable(v[k])}`).join(",")}}`:JSON.stringify(v),h=v=>crypto.createHash("sha256").update(stable(v)).digest("hex"),binding={artifactId:"mer_histbootstrap_71c280422c057da3b248a43b",artifactDigest:"a".repeat(64),asOf:"2026-09-11T12:00:00Z",sourceId:"DATAFORSEO_GOOGLE_SHOPPING",products:["p1","p2","p3"],taskCap:9,spendCapUsd:.009,utcDayCapUsd:.01},cp={schemaVersion:"1.0",checkpointId:"cp",policyVersion:HISTORICAL_BOOTSTRAP_CHECKPOINT_VERSION,binding,bindingDigest:h(binding),createdAt:"2026-09-11T12:00:00Z",events:[{sequence:1,type:"CHECKPOINT_PREPARED",at:"2026-09-11T12:00:00Z",productIndex:0,atlasProductId:"p1",stage:"PRODUCTS"}]};let current=structuredClone(cp),savedAuth,calls=0,repo={create:async x=>(current=x),getById:async()=>structuredClone(current),append:async(_,e)=>(current={...current,events:[...current.events,{...e,sequence:current.events.length+1}]})},service=new HistoricalBootstrapLifecycleService({artifactRepository:{getById:async()=>null},checkpointRepository:repo,continuationRepository:{record:async a=>(savedAuth=a),getById:async()=>savedAuth},handoff:{execute:async x=>(calls++,{status:"PAID_TASK_CREATED",...x})},retrievalOwner:{retrieve:async()=>({status:"PENDING"})},identityOwner:{resolve:async()=>({status:"STRONG_UNIQUE"})},localProcessingOwner:{process:async()=>({status:"ADMITTED"})},rightsRegistry:{get:()=>({acquisition:{api:"ALLOWED"}})},spendResolver:async()=>0,now:()=>"2026-09-11T12:01:00Z"});const inspected=await service.inspect({checkpointId:"cp"});assert.equal(inspected.nextPaidOperation,"PRODUCTS");assert.equal(inspected.providerOperation,"NONE");const auth=await service.authorizeNext({checkpointId:"cp",authorizedBy:"operator:test",reason:"fixture",confirmation:"AUTHORIZE-PRODUCTS-cp"});assert.equal(auth.atlasProductId,"p1");assert.equal(auth.nextOperation,"PRODUCTS");await assert.rejects(()=>service.authorizeNext({checkpointId:"cp",authorizedBy:"x",reason:"x",confirmation:"AUTHORIZE-PRODUCTS-cp",paidActionIntentId:"mer_histbootintent_"+"a".repeat(24)}),/CALLER_OVERRIDE/);await assert.rejects(()=>service.executeNext({checkpointId:"cp",authorizationId:auth.continuationAuthorizationId,executedBy:"x",confirmation:auth.confirmation,atlasProductId:"p2"}),/CALLER_OVERRIDE/);await service.executeNext({checkpointId:"cp",authorizationId:auth.continuationAuthorizationId,executedBy:"x",confirmation:auth.confirmation});assert.equal(calls,1);current={...current,events:[...current.events,{sequence:2,type:"TASK_CREATED",at:"x",productIndex:0,atlasProductId:"p1",operation:"PRODUCTS",providerTaskId:"task",actualSpendUsd:.001}]};const pending=await service.retrieve({checkpointId:"cp"});assert.equal(pending.status,"PENDING");assert.equal(current.events.length,2);assert.equal(projectHistoricalBootstrapCheckpoint({checkpoint:current}).cohortState,"WAITING_FOR_RESULT");console.log("Historical bootstrap trusted lifecycle tests passed (12 cases).");
