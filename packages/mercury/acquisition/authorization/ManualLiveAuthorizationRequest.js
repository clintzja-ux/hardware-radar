import crypto from 'node:crypto';
export function createManualLiveAuthorizationRequest({plan,createdAt=new Date().toISOString(),ttlMinutes=15}={}){if(!plan?.planId)throw new TypeError('plan is required.');if(plan.approvedTaskCount!==1)throw new Error('MANUAL_LIVE_REQUIRES_EXACTLY_ONE_APPROVED_TASK');if(plan.estimatedApprovedSpendUsd>0.001)throw new Error('MANUAL_LIVE_SPEND_CEILING_EXCEEDED');const requestId=`liveauth_${crypto.createHash('sha256').update(`${plan.planId}|${createdAt}`).digest('hex').slice(0,24)}`;return Object.freeze({schemaVersion:'1.0',requestId,mode:'LIVE',planId:plan.planId,createdAt,expiresAt:new Date(Date.parse(createdAt)+ttlMinutes*60000).toISOString(),maxSpendUsd:0.001,maxPaidTasks:1,automaticPaidRetries:0,status:'PENDING_OPERATOR_APPROVAL',plan})}
