function freeze(x){if(x&&typeof x==="object"&&!Object.isFrozen(x)){Object.freeze(x);for(const v of Object.values(x))freeze(v);}return x;}
function recordsForProduct(records,id){return records.filter(r=>r.candidate?.identity?.atlasProductId===id);}
export function prepareProductIdentityReview({records,atlasProductId,preparedAt,requestId}={}){const matched=recordsForProduct(records??[],atlasProductId);if(!matched.length||matched.some(r=>r.candidate?.identity?.outcome!=="PROBABLE"))throw new Error("PRODUCT_REVIEW_REQUIRES_PROBABLE_RETAINED_EVIDENCE");return freeze({schemaVersion:"1.0",requestId,subjectType:"PRODUCT_IDENTITY",atlasProductId,previousState:"PROBABLE",requestedState:"VERIFIED",supportingEvidenceReferences:matched.map(r=>r.evidenceId).sort(),status:"PENDING_OPERATOR_REVIEW",preparedAt,paidTaskCreated:false,actualSpendUsd:0});}
export function prepareMerchantIdentityReview({records,canonicalDomain,canonicalMerchantName,merchantId,preparedAt,requestId}={}){const matched=(records??[]).filter(r=>r.merchantResolution?.canonicalDomain===canonicalDomain);if(!matched.length||matched.some(r=>r.merchantResolution?.outcome!=="DISCOVERED"))throw new Error("MERCHANT_REVIEW_REQUIRES_DISCOVERED_RETAINED_EVIDENCE");if(typeof merchantId!=="string"||!merchantId.trim())throw new TypeError("merchantId is required.");return freeze({schemaVersion:"1.0",requestId,subjectType:"MERCHANT_IDENTITY",discoveredMerchantName:matched[0].merchantResolution.sellerName,canonicalMerchantName,canonicalDomain,merchantId,previousState:"DISCOVERED",requestedState:"REGISTERED",supportingEvidenceReferences:matched.map(r=>r.evidenceId).sort(),status:"PENDING_OPERATOR_REVIEW",preparedAt,paidTaskCreated:false,actualSpendUsd:0});}
