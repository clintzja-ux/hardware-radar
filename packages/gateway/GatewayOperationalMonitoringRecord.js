import {GATEWAY_OPERATIONAL_CATEGORIES,GATEWAY_SAFE_ERROR_CLASSIFICATIONS} from "./GatewayOperationalMonitoringPolicy.js";
const allowedKeys=["schemaVersion","operationalEventId","category","recordedAt","routeClassification","httpStatusCode","durationMs","errorClassification","runtimeClassification","storageOutcome"],routes=["BEACON_PRODUCT_INTEREST_WRITE","BEACON_HEALTH"],runtimes=["CLOUDFLARE_WORKER","FIXTURE_RUNTIME"],storage=["NOT_APPLICABLE","ACCEPTED","DUPLICATE","CONFLICT","FAILURE"],nonBlank=value=>typeof value==="string"&&value.trim()!=="",freeze=value=>{if(value&&typeof value==="object"&&!Object.isFrozen(value)){Object.freeze(value);for(const child of Object.values(value))freeze(child);}return value;};
export function createGatewayOperationalMonitoringRecord(input={}){const optional=(key,valid)=>!(key in input)||valid(input[key]);if(!input||typeof input!=="object"||Array.isArray(input)||input.schemaVersion!=="1.0"||!/^gwop_[A-Za-z0-9_-]{8,96}$/.test(input.operationalEventId)||!GATEWAY_OPERATIONAL_CATEGORIES.includes(input.category)||!nonBlank(input.recordedAt)||!Number.isFinite(Date.parse(input.recordedAt))||!routes.includes(input.routeClassification)||Object.keys(input).some(key=>!allowedKeys.includes(key))||!optional("httpStatusCode",value=>Number.isSafeInteger(value)&&value>=100&&value<=599)||!optional("durationMs",value=>typeof value==="number"&&Number.isFinite(value)&&value>=0&&value<=600000)||!optional("errorClassification",value=>GATEWAY_SAFE_ERROR_CLASSIFICATIONS.includes(value))||!optional("runtimeClassification",value=>runtimes.includes(value))||!optional("storageOutcome",value=>storage.includes(value)))throw new TypeError("GATEWAY_OPERATIONAL_MONITORING_RECORD_INVALID");return freeze(structuredClone(input));}
export default createGatewayOperationalMonitoringRecord;
