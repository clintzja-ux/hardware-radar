export const BEACON_PRODUCTION_STORAGE_REQUIREMENTS=Object.freeze(["durable","atomicCompareInsert","uniqueEventId","concurrentWriters","processIndependent","deterministicList","constraintFailure","backupRecovery","retentionDeletionCapability"]);
const freeze=value=>{if(value&&typeof value==="object"&&!Object.isFrozen(value)){Object.freeze(value);for(const child of Object.values(value))freeze(child);}return value;};
export function assessBeaconProductionStorage(storage=null){if(storage===null)return freeze({configured:false,ready:false,adapter:null,missing:[...BEACON_PRODUCTION_STORAGE_REQUIREMENTS]});if(typeof storage!=="object"||Array.isArray(storage)||typeof storage.adapter!=="string"||!storage.adapter.trim())throw new TypeError("BEACON_PRODUCTION_STORAGE_DESCRIPTOR_INVALID");const missing=BEACON_PRODUCTION_STORAGE_REQUIREMENTS.filter(key=>storage[key]!==true);return freeze({configured:true,ready:missing.length===0,adapter:storage.adapter.trim(),missing});}
